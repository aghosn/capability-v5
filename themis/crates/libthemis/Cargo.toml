[package]
name    = "libthemis"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[features]
default = []
ffi     = []       # Enable C-FFI shims + panic handler for standalone staticlib builds

[dependencies]
themis-abi = { path = "../themis-abi" }
